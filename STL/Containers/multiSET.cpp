#include <bits/stdc++.h>
using namespace std;

void mset(){
    // A multiset in STL is an associative container similar to a set, but it allows duplicate elements.

};


int main() {
    mset();
    return 0;
}